function pickshape () {
    hand = randint(1, 3)
    if (true) {
    	
    }
    if (true) {
    	
    }
    if (true) {
    	
    }
}
let hand = 0
radio.setGroup(1)
let rockindex = 1
let paperindex = 2
let scissorsindex = 3
let paper = images.createImage(`
    # # # # #
    # . . . #
    # . . . #
    # . . . #
    # # # # #
    `)
let rock = images.createImage(`
    . . . . .
    . # # # .
    # # # # #
    # # # # #
    . . . . .
    `)
let scissors = images.createImage(`
    # # . . #
    # # . # .
    . . # . .
    # # . # .
    # # . . #
    `)
